export function Warn(message) {
    ui.notifications.warn(`Fate Hybrid Skills | ${message}`);
}

export function Error(message) {
    ui.notifications.error(`Fate Hybrid Skills | ${message}`);
}

export function Info(message) {
    ui.notifications.info(`Fate Hybrid Skills | ${message}`);
}